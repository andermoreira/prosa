name: lint

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: lint-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0

      - uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af # v4.1.0
        with:
          node-version: 22

      - name: Instalar dependências npm sem lifecycle scripts
        run: npm ci --ignore-scripts

      - name: Audit de dependências npm (high)
        run: npm audit --audit-level=high

      - name: Verificação hermética do workflow
        run: npm run verify

      - name: Secret scanning (gitleaks)
        run: |
          archive=/tmp/gitleaks_8.24.2_linux_x64.tar.gz
          curl -fsSL -o "$archive" \
            "https://github.com/gitleaks/gitleaks/releases/download/v8.24.2/gitleaks_8.24.2_linux_x64.tar.gz"
          echo "fa0500f6b7e41d28791ebc680f5dd9899cd42b58629218a5f041efa899151a8e  $archive" \
            | sha256sum -c -
          tar xzf "$archive" -C /tmp gitleaks
          sudo mv /tmp/gitleaks /usr/local/bin/gitleaks
          gitleaks detect --source . --no-banner --redact --config .gitleaks.toml
